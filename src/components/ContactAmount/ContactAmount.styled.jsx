import styled from '@emotion/styled';

const TotalContactsText = styled.p`
  font-size: 18px;
`;

const TotalContactsNum = styled.span`
  margin-left: 4px;
  color: blue;
`;

export { TotalContactsNum, TotalContactsText };